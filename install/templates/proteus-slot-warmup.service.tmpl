[Unit]
Description=Warmup Proton slot NAT state (one pass over all rotating slots)
After=proteus-dispatcher.service
Wants=proteus-dispatcher.service

[Service]
Type=oneshot
ExecStart=/etc/proteus/bin/slot-warmup.sh
# Quiet: the script logs via syslog already
StandardOutput=null
StandardError=journal
