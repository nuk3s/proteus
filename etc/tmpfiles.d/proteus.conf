# /run/proteus: setgid so files the dispatcher/rotation scripts create
# (dispatcher-status.json, rotation-history.jsonl, trigger files) inherit
# group proteus-ui without those processes needing CAP_CHOWN.
d /run/proteus 2750 root proteus-ui -
