# Runtime / secrets — never commit
# WG configs contain private keys
etc/proteus/wg/
# Per-tunnel state (peer pubkeys, endpoints)
etc/proteus/state/
# Proton SSO refresh token
etc/Proton/

# Backups / editor noise
*.bak
*.bak-*
*~
__pycache__/
*.pyc
install/proteus.conf
install/.staging/
